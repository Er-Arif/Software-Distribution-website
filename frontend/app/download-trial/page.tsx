import { Shell } from "@/components/ui";

export default function TrialDownloadPage() {
  return (
    <Shell>
      <section className="mx-auto max-w-4xl px-6 py-12">
        <h1 className="text-3xl font-bold">Download Trial</h1>
        <p className="mt-3 text-slate-600">
          Trial downloads are protected by product settings and download policies. Published trial builds appear here once enabled by an admin.
        </p>
      </section>
    </Shell>
  );
}
